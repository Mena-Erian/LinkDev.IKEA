@using LinkDev.IKEA.PL.Controllers
@model EmployeeListViewModel
@{
	ViewData["Title"] = "Employees";
	var temp = Model.Employees.FirstOrDefault(new EmployeeViewModel()
	{
		CreatedBy = "",
		FullName = "",
		LastModifiedBy = ""
	});
	var prevDisabled = Model.Page == 1 ? "disabled" : ""; var nextDisabled = Model.Page * Model.PageSize >= Model.TotalCount ? "disabled" : ""; var totalPages = (int)Math.Ceiling(Model.TotalCount / (double)Model.PageSize);

}


<h1>All Employees</h1>
<br />
<br />
<br />
@if (Model.Employees.Any())
{

	<div class="row mb-3">
		<div class="col-md-6">
			<a class="btn btn-primary" asp-controller="Employee" asp-action="Create">Create Employee</a>
		</div>
		<div class="col-md-6">
			<form asp-action="Index" method="get" class="form-inline float-right">
				<div class="input-group">
					<input type="text" asp-for="SearchTerm" class="form-control" placeholder="Search employees...">
					<div class="input-group-append">
						<button type="submit" class="btn btn-outline-secondary">Search</button>
					</div>
				</div>

				<div class="col-auto">
					<label for="sortBy" class="col-form-label">Sort By:</label>
				</div>

				<div class="col-auto">
					<select id="sortBy" asp-for="SortBy" class="form-select">
						<option value="name">Full Name</option>
						<option value="email">Email</option>
						<option value="hireDate">Hire Date</option>
						<option value="department">Department</option>
						<option value="status">status</option>
					</select>
				</div>

				<div class="col-auto">
					<div class="form-check">
						<input class="form-check-input" type="checkbox" id="sortAscending" asp-for="SortAscending" />
						<label class="form-check-label" asp-for="SortAscending">
							Ascending Order
						</label>
					</div>
				</div>

				<div class="col-auto">
					<button class="btn btn-outline-primary" type="submit">Apply Sort</button>
				</div>
			</form>
		</div>
	</div>

	<table class="table table-striped table-hover mt-3">
		<thead class="text-center">
			<tr>
				<th>@Html.DisplayNameFor(_ => temp.FullName)</th>
				<th>@Html.DisplayNameFor(_ => temp.Email)</th>
				<th>@Html.DisplayNameFor(_ => temp.EmployeeType)</th>
				<th>@Html.DisplayNameFor(_ => temp.Age)</th>
				<th>@Html.DisplayNameFor(_ => temp.Salary)</th>
				<th>Status</th>
				<th>De/Activiate</th>
				<th>@Html.DisplayNameFor(_ => temp.Gender)</th>
				<th>@Html.DisplayNameFor(_ => temp.FormattedHireDate)</th>
				<th>Details </th>
				<th>Update</th>
				<th>Delete</th>
			</tr>
		</thead>

		<tbody class="text-center">
			@foreach (var employee in Model.Employees)
			{
				<tr>

					<td>@employee.FullName</td>
					<td>@employee.Email</td>
					<td>@employee.EmployeeType</td>
					<td>@employee.Age</td>
					<td>@employee.Salary</td>
					<td>
						@if (employee.IsActive)
						{
							<button class="btn btn-success btn-sm">
								Active
							</button>
						}
						else
						{
							<button class="btn btn-secondary btn-sm">
								Inactive
							</button>
						}
					</td>
					<td>
						<form asp-action="@nameof(EmployeeController.ToggleStatus)" asp-route-id="@employee.Id" asp-route-activation="@(employee.IsActive ? false : true)" method="post" style="display:inline;">
							<input type="hidden" asp-for="@employee.IsActive" />
							<button type="submit" class="btn btn-outline-dark btn-sm">
								@(employee.IsActive ? "Deactivate" : "Activate")
							</button>
						</form>
					</td>
					<td>@employee.Gender</td>
					<td>@employee.FormattedHireDate</td>

					@*Render Partial View*@

					@* @Html.Partial("/LinkDev.IKEA.PL/Views/Employee/_ButtonsPartialView.cshtml", employee.Id); *@
					<partial name="_ButtonsPartialView" model="@employee.Id.ToString()"></partial>

					<td>
						<button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal-@employee.Id">
							<i class="fas fa-trash"></i> Delete
						</button>
						<!-- Delete Modal -->
						<div class="modal fade" id="deleteModal-@employee.Id" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
							<div class="modal-dialog">
								<div class="modal-content">
									<div class="modal-header">
										<h1 class="modal-title fs-5" id="exampleModalLabel">Delete Employee</h1>
										<button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
									</div>
									<div class="modal-body">
										Are You Sure to Delete this Employee?
									</div>
									<div class="modal-footer">
										<form method="post" asp-action="Delete" asp-route-id="@employee.Id" class="mt-4">
											<button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
											<input type="submit" value="Delete" class="btn btn-danger" />
										</form>
									</div>
								</div>
							</div>
						</div>
					</td>

				</tr>
			}
		</tbody>
	</table>



	<!-- Pagination -->


	<div class="d-flex justify-content-between align-items-center">
		<div>
			Showing @(((Model.Page - 1) * Model.PageSize) + 1) to @Math.Min(Model.Page * Model.PageSize, Model.TotalCount) of @Model.TotalCount employees
		</div>

		<nav aria-label="Page navigation">
			<ul class="pagination">
				<li class="page-item @prevDisabled">
					<a class="page-link" asp-action="Index" asp-route-page="@(Model.Page - 1)" asp-route-pageSize="@Model.PageSize" asp-route-sortBy="@Model.SortBy" asp-route-sortAscending="@Model.SortAscending" asp-route-searchTerm="@Model.SearchTerm">
						Previous
					</a>
				</li>

				@for (int i = Math.Max(1, Model.Page - 2); i
							<= Math.Min(totalPages, Model.Page + 2); i++)
				{
					<li class="page-item @(i == Model.Page ? " active " : " ")">
						<a class="page-link" asp-action="Index" asp-route-page="@i" asp-route-pageSize="@Model.PageSize" asp-route-sortBy="@Model.SortBy" asp-route-sortAscending="@Model.SortAscending" asp-route-searchTerm="@Model.SearchTerm">
							@i
						</a>
					</li>
				}

				<li class="page-item @nextDisabled">
					<a class="page-link" asp-action="Index" asp-route-page="@(Model.Page + 1)" asp-route-pageSize="@Model.PageSize" asp-route-sortBy="@Model.SortBy" asp-route-sortAscending="@Model.SortAscending" asp-route-searchTerm="@Model.SearchTerm">
						Next
					</a>
				</li>
			</ul>
		</nav>
	</div>
}
else
{
	<div class="alert alert-info mt-3">
		<h2>No Employees Found !!</h2>
	</div>
}


@*   <div class="container-fluid">
        <h1>Employees</h1>

        <div class="row mb-3">
            <div class="col-md-6">
                <a asp-action="Create" class="btn btn-primary">Create New</a>
            </div>
            <div class="col-md-6">
                <form asp-action="Index" method="get" class="form-inline float-right">
                    <div class="input-group">
                        <input type="text" name="searchTerm" value="@Model.SearchTerm" class="form-control" placeholder="Search employees...">
                        <div class="input-group-append">
                            <button type="submit" class="btn btn-outline-secondary">Search</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>

        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead class="thead-dark">
                    <tr>
                        <th>
                            <a asp-action="Index" asp-route-sortBy="name" asp-route-sortAscending="@(Model.SortBy == " name " ? !Model.SortAscending : true)" asp-route-page="@Model.Page" asp-route-pageSize="@Model.PageSize" asp-route-searchTerm="@Model.SearchTerm">
                                Full Name
                                @if (Model.SortBy == "name")
                                {
                                    <i class="fas fa-sort-@(Model.SortAscending ? "up" : "down")"></i>
                                }
                            </a>
                        </th>
                        <th>
                            <a asp-action="Index" asp-route-sortBy="email" asp-route-sortAscending="@(Model.SortBy == " email " ? !Model.SortAscending : true)" asp-route-page="@Model.Page" asp-route-pageSize="@Model.PageSize" asp-route-searchTerm="@Model.SearchTerm">
                                Email
                                @if (Model.SortBy == "email")
                                {
                                    <i class="fas fa-sort-@(Model.SortAscending ? "up" : "down")"></i>
                                }
                            </a>
                        </th>
                        <th>
                            <a asp-action="Index" asp-route-sortBy="department" asp-route-sortAscending="@(Model.SortBy == " department " ? !Model.SortAscending : true)" asp-route-page="@Model.Page" asp-route-pageSize="@Model.PageSize" asp-route-searchTerm="@Model.SearchTerm">
                                Department
                                @if (Model.SortBy == "department")
                                {
                                    <i class="fas fa-sort-@(Model.SortAscending ? "up" : "down")"></i>
                                }
                            </a>
                        </th>
                        <th>Status</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.Employees)
                    {
                        <tr>
                            <td>@item.FullName</td>
                            <td>@item.Email</td>
                            <td>@item.Department</td>
                            <td>
                                @if (item.IsActive)
                                {
                                    <span class="badge badge-success">Active</span>
                                }
                                else
                                {
                                    <span class="badge badge-secondary">Inactive</span>
                                }
                            </td>
                            <td>
                                <div class="btn-group">
                                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info">
                                        <i class="fas fa-info-circle"></i> Details
                                    </a>
                                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning">
                                        <i class="fas fa-edit"></i> Edit
                                    </a>
                                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">
                                        <i class="fas fa-trash"></i> Delete
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <!-- Pagination -->
        @{
            var prevDisabled = Model.Page == 1 ? "disabled" : ""; var nextDisabled = Model.Page * Model.PageSize >= Model.TotalCount ? "disabled" : ""; var totalPages = (int)Math.Ceiling(Model.TotalCount / (double)Model.PageSize);
        }

        <div class="d-flex justify-content-between align-items-center">
            <div>
                Showing @(((Model.Page - 1) * Model.PageSize) + 1) to @Math.Min(Model.Page * Model.PageSize, Model.TotalCount) of @Model.TotalCount employees
            </div>

            <nav aria-label="Page navigation">
                <ul class="pagination">
                    <li class="page-item @prevDisabled">
                        <a class="page-link" asp-action="Index" asp-route-page="@(Model.Page - 1)" asp-route-pageSize="@Model.PageSize" asp-route-sortBy="@Model.SortBy" asp-route-sortAscending="@Model.SortAscending" asp-route-searchTerm="@Model.SearchTerm">
                            Previous
                        </a>
                    </li>

                    @for (int i = Math.Max(1, Model.Page - 2); i
                                    <= M ath.Min(totalPages, Model.Page + 2); i++)
                    {
                        <li class="page-item @(i == Model.Page ? " active " : " ")">
                            <a class="page-link" asp-action="Index" asp-route-page="@i" asp-route-pageSize="@Model.PageSize" asp-route-sortBy="@Model.SortBy" asp-route-sortAscending="@Model.SortAscending" asp-route-searchTerm="@Model.SearchTerm">
                                @i
                            </a>
                        </li>
                    }

                    <li class="page-item @nextDisabled">
                        <a class="page-link" asp-action="Index" asp-route-page="@(Model.Page + 1)" asp-route-pageSize="@Model.PageSize" asp-route-sortBy="@Model.SortBy" asp-route-sortAscending="@Model.SortAscending" asp-route-searchTerm="@Model.SearchTerm">
                            Next
                        </a>
                    </li>
                </ul>
            </nav>
        </div>
    </div>
    @if (!string.IsNullOrEmpty(TempData["ErrorMessage"] as string))
    {
        <div class="position-fixed bottom-0 end-0 p-3" style="z-index: 11">
            <div id="liveToast" class="toast show" role="alert" aria-live="assertive" aria-atomic="true">
                <div class="toast-header">
                    <strong class="me-auto">IKEA</strong>
                    <button type="button" class="btn-close" data-bs-dismiss="toast" aria-label="Close"></button>
                </div>
                <div class="toast-body">
                    @TempData["ErrorMessage"]
                </div>
            </div>
        </div>
    }
}
*@
@if (!string.IsNullOrEmpty(TempData["Message"] as string))
{
	<div class="position-fixed bottom-0 end-0 p-3" style="z-index: 11">
		<div id="liveToast" class="toast show" role="alert" aria-live="assertive" aria-atomic="true">
			<div class="toast-header">
				<strong class="me-auto">IKEA</strong>
				<button type="button" class="btn-close" data-bs-dismiss="toast" aria-label="Close"></button>
			</div>
			<div class="toast-body">
				@TempData["Message"]
			</div>
		</div>
	</div>
}